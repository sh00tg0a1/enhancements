# 📄 KWeaver 设计文档（KEP 风格）

## 📌 背景

KWeaver 正在扩展到多个复杂子系统——Ontology Data Flow、Data Agent、Sandbox、分布式执行等。为保持架构一致、促进协作、让重大决策可追溯，我们采用结构化的设计文档流程。

KWeaver 采用 **KEP（Kubernetes Enhancement Proposal）风格** 的增强与设计文档治理模型。

---

## 🧩 什么是 KEP？

KEP = **增强提案 + 设计文档**

最初由 Kubernetes 社区使用，用于管理：

* 新功能提案
* 架构设计
* 技术改进
* 大型系统变更
* 社区讨论与审阅

这是长期演化、多贡献者项目的成熟治理模式。

---

## 🏆 为何选择 KEP？

| KWeaver 需要 | KEP 能提供 |
| --- | --- |
| 协作式设计审阅 | PR + 逐行评论 |
| 统一的文档规范 | 单一模板与结构 |
| 可追溯的演进 | 版本化文档与状态 |
| 架构有序 | 明确目标/非目标、风险、替代方案 |
| 贡献者快速上手 | 每次变更都有完整上下文 |
| 审计友好 | 决策历史可被引用 |

适用于涵盖以下模块的系统：

* Ontology Data Flow
* Data Agent
* Sandbox
* 分布式流水线与执行
* DSL / 查询引擎
* 多模块依赖

---

## 📁 KEP 目录结构

```text
docs/
└── keps/
    ├── 0001-core-architecture.md
    ├── 0002-dsl-v2.md
    ├── 0003-data-agent.md
    ├── 0004-task-pipeline-v2.md
    └── templates/
        └── kep_template.md
```

说明：

* `0001`、`0002` 等为有序编号，便于稳定排序
* 每个文件是一份完整设计文档
* `templates/kep_template.md` 为标准模板
* 所有 KEP 通过 PR 审阅，讨论与修改留存在 GitHub

---

## 🔄 工作流

### 1）提出设计 Issue

创建 GitHub Issue：

```text
[KEP] <功能名称>
```

内容应包含：

* 背景 / 原因
* 待解决的问题
* 初步想法（草稿）

### 2）创建 KEP 文档

从模板复制：

```text
docs/keps/templates/kep_template.md
```

保存为：

```text
docs/keps/00xx-<short-title>.md
```

编号按顺序分配。

### 3）通过 Pull Request 提交（核心步骤）

PR 标题示例：

```text
KEP-0003: Data Agent Design
```

PR 用于：

* 讨论
* 逐行审阅
* 提问
* 更新设计

### 4）管理状态（保持透明）

状态字段：

```text
Status: Draft / Review / Implementable / Implemented / Deprecated
```

含义示例：

* Draft：初稿
* Review：审阅中
* Implementable：可实施
* Implemented：已上线
* Deprecated：已废弃

### 5）实现需遵循 KEP

当状态为 **Implementable**：

* 相关代码 PR 必须引用该 KEP
* 实现需与设计一致
* 若有冲突先更新 KEP 再改代码

### 6）长期追踪

发布时可列出已交付的 KEP，例如：

```text
KWeaver v0.3 交付：
* KEP-0002 DSL V2
* KEP-0004 Task Pipeline V2
```

让文档成为长期资产。

---

## 📦 使用 KEP 风格的项目案例

* Kubernetes: `https://github.com/kubernetes/enhancements`
* Istio: `https://github.com/istio/enhancements`
* Knative: `https://github.com/knative/enhancements`
* OpenTelemetry (OTEPs): `https://github.com/open-telemetry/oteps`
* Helm (HIPs): `https://github.com/helm/community/tree/master/hips`
* Rust (RFCs): `https://github.com/rust-lang/rfcs`
* VS Code（API 提案 / 功能 RFC）: `https://github.com/microsoft/vscode/issues?q=label%3Aapi-proposal`

这些项目展示了如何组织提案、讨论、状态与实现链接。

---

## 📄 KEP 模板（简化版）

位置：

```text
docs/keps/templates/kep_template.md
```

模板内容：

```text
# KEP-<编号>: <标题>

- Author(s):
- Status: Draft / Review / Implementable / Implemented / Deprecated
- Created: YYYY-MM-DD
- Last Updated:
- Discussion PR:

## 1. 背景与动机
为何需要此设计？当前存在哪些问题？

## 2. 目标
希望达成的结果。

## 3. 非目标
明确不在范围内的内容。

## 4. 高层方案
架构、数据流、模块边界。

## 5. 技术细节
- API / 接口
- 数据结构
- 状态机
- 执行流程
- 兼容性与迁移

## 6. 风险 / 取舍

## 7. 替代方案

## 8. 里程碑

## 9. 参考资料
```
